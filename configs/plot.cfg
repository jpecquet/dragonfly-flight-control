# Plot config: Generate visualization from simulation output

# Input HDF5 file from simulation
input = output.h5

# Output image or video
# Use .png for static image, .mp4 for animation
output = output.mp4

# Optional: Python interpreter override for postprocessing.
# Resolution order in `dragonfly plot`:
# 1) this `python_executable` value
# 2) `PYTHON` environment variable
# 3) fallback `python3`
# python_executable = /Users/jean/.pyenv/shims/python

# Optional: force matplotlib-only fallback (skip Blender)
# no_blender = true
